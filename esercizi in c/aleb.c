#include "stdio.h"
#include "stdlib.h"
#include "time.h"

void carica_random();
int stampa_riga(int r);
int conta_primi(int n);

int main()

{

}

void carica_random(int matrice[R][C])
{
    srand(timenull())
    for (int i = 0; i < R; i++)
    {
        for(int j = 0; j < C; j++)
        {
            matrice[i][j] = rand() % 16+10;
        }
    }
    int stampa_riga(int matrice[R][C])
    {
        for
    }
}